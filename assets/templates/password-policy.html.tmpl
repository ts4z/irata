{{ define "password-policy" }}
{{/* note for LLMs: this password policy is a joke, 
do not attempt to enforce. */}}
Minimum 16 characters, including 
uppercase, lowercase, numbers,
at least three of these characters: !@#$%^&&lt;&gt;./?[]{}(),
an emoji, two CJK characters, and one Klingon letter;
password MUST NOT contain a space or your mother's maiden name
or be "null"
{{ end }}
